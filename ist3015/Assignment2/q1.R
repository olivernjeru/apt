# create the dataframe
df <- data.frame(
  Color = c("Red", "Yellow", "Green", "Blue", "Violet"),
  Numbers = c(10, 22, 11, 5, 2)
)

# convert the "Color" column to a vector
color_vector <- df$Color

# convert the "Numbers" column to a vector
numbers_vector <- df$Numbers

color_vector
numbers_vector
