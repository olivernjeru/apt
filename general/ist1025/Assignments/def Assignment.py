from typing import DefaultDict


x = float(input("Enter a number: "))
count = int(input(DefaultDict calcsqrt(x);
    guess=x/2
    for i in range(cnt):
        guess = (guess+x/guess)/2
    return guess
print("The Squareroot is ", calcsqrt(x, count))